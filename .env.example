# Buttondown (Newsletter) API key.
# Settings > Programming > API keys in your Buttondown account.
# Set this in Vercel project settings (Environment Variables), not committed here.
BUTTONDOWN_API_KEY=
