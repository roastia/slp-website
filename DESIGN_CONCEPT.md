# SLP ウェブサイト デザインコンセプト

このドキュメントは、Claudeとのやりとりの中で決まったデザインの方向性・経緯をまとめたものです。
以降このサイトのデザインを進める際は、以下の内容を踏まえて作業してください。

## プロジェクト概要

- **サイト名 / レーベル名**: SLP（福岡拠点のレコードレーベル）
- **既存サイト**: https://www.slprecordings.com/ （Diviで構築されたWordPressサイト）
- **今回の目的**: 既存サイトのデザインが古く感じられるため、よりクールでミニマルなデザインに刷新する
- **技術方針**: WordPress（CMS）はやめて、完全な静的HTML + CSS + 最小限のJavaScriptのみで構築する方針に変更済み
- **ジャンル**: テクノ、アンビエント、ポストロック、ミニマル、ポップ、エレクトロ、エレクトロニカ、ダンス、ワールド
- **所属アーティスト**: impressionists、KEIMABASS、swmcps（3組）
- **リリース数**: 現時点で8作品

## デザインの方向性（最重要）

「**ミニマル・IDM・デトロイトテクノなどのシンプルでクールなダンスミュージックを体現するような、クールで洗練されたデザイン**」というのが最初のリクエスト。参考として https://raster-media.net/ （raster-noton系列の実験音楽レーベルサイト。モノクロ・モノスペース書体・余白の多いリスト表示が特徴）を挙げられ、そのオマージュとして作成した。

### 色

- **白と黒の2色のみ**で構成する（人間から明確な指示あり）
- アクセントカラー（蛍光グリーンなど）は**意図的に使わない**。AIが作りがちな「黒背景+アクセント1色」という定番構成を避けるため
- 階層づけは色ではなく、**白背景⇄黒背景の反転（パネル切り替え）**、余白の大小、文字の太さ・サイズで行う

### 罫線・線について（重要な経緯）

- 当初はセクション区切りに1本線の罫線を使っていたが「ダサい」とフィードバックがあり、点線（ティックパターン）に変更
- その後、点線も「ダサい」と指摘され、**最終的に線を一切使わない方針**に確定
- 現在の区切り手法は以下の3つのみ：
  1. **余白**（セクション間の間隔）
  2. **白黒の反転**（背景色が変わること自体を区切りとする）
  3. **タイポグラフィの強弱**（太字・広い字間・大文字の見出しラベル）
- **今後、罫線・下線・点線などの「線」で区切りを表現するデザインは避けること**

### フォント

- 当初は Space Grotesk（見出し）+ IBM Plex Mono（データ表示用の等幅） + Inter（本文）の3書体構成だった
- ユーザーから「このフォントは何か」と実例画像（raster-media.netの"milian mori triality"という見出し）を見せられ、調査の結果 **Söhne（ゾーネ、Klim Type Foundry）** と判明。ただし有料フォントのため、無料の近似書体として **General Sans**（Fontshare提供、無料）を採用
- 最終的に、**見出し・本文・データ表示まですべて General Sans 1書体に統一**（IBM Plex Monoも含めて全て置き換え済み）
- Fontshare CDN経由で読み込み: `https://api.fontshare.com/v2/css?f[]=general-sans@400,500,600,700&display=swap`

### レイアウト・構成

- Catalog（作品一覧）とArtists（アーティスト一覧）は、**同じグリッドシステム（`minmax(150px, 1fr)`の自動グリッド）で統一**し、タイルサイズを揃えている
- 画像は**カラーのまま使用**（グレースケール加工はしない、との指示あり）
- ホバー時の見せ方: 画像を少し拡大（`scale(1.05)`）＋タイトル/名前部分が黒背景に反転する、という統一されたインタラクション言語
- 全体的に「コンパクト」を志向しており、上下の余白（padding）は繰り返し縮小されてきた経緯がある（当初80px→現在は48px程度が基準）
- カタログ番号（001, 002...）や件数表示（"008 releases"「3 acts」など）は**不要と判断され削除済み**。今後も無意味な数字ラベルは付けない
- Heroの説明文（"record label — signal from fukuoka. techno, ambient..."）は**折り返さず1行で表示**する指定あり

### ページ構成（静的サイト版）

- `index.html`（Home）: ヒーロー＋最新リリースの黒反転パネル
- `catalog.html`: 全リリースのグリッド
- `artists.html`: アーティスト一覧のグリッド
- `movie.html`: YouTube動画のグリッド（実際に埋め込み再生可能なiframe）
- `about.html`: レーベル紹介（黒反転パネル、全幅）
- `contact.html`: 問い合わせ先（静的サイトのためフォームは無く、mailtoリンクのみ）
- `artist-detail.html`: アーティスト詳細ページを量産するためのデフォルトテンプレート
- `catalog-detail.html`: 作品詳細ページを量産するためのデフォルトテンプレート
- `nav.js`: ヘッダー・フッターを一元管理する共通スクリプト（1箇所直せば全ページに反映される）
- `style.css`: 全ページ共通スタイル

## 現状の既知の制約・未対応事項

- Contactページに実際に送信できるフォームがない（静的HTMLのため）。Googleフォームや Formspree 等の外部サービスとの連携が今後の課題
- 画像は現行WordPressサイト（slprecordings.com）のメディアURLをそのまま参照している状態。WordPressを完全に廃止する場合は、画像を別途ダウンロードして同じサーバー上に配置し直す必要がある
- レスポンシブ対応（viewportメタタグ含む）は基本的に検証済みだが、実機での最終確認は未実施

## 2026-08 デザイン深化：モーションとインタラクション

既存のモノクロ・ミニマルという軸を維持したまま、装飾を増やすのではなく「操作への反応」で洗練させる方針を追加した。

- ページ表示時と内部ページ移動時に、短く控えめなフェードを使用する
- コンテンツはスクロールに合わせてフェード＋わずかな移動で表示する。大きく跳ねる、回転するなどの派手な演出は使わない
- Homeの大きな「SLP」は、ポインター位置に対して数pxだけ追従する
- Catalog / Artistsの共通グリッドとカード寸法は維持し、画像拡大と白黒反転のホバー表現のみ精度を上げる
- ヘッダーに「MOTION ON / OFF」を設置し、利用者がすべての演出を止められるようにする。選択状態はブラウザ内に保存する
- `prefers-reduced-motion` を尊重し、OS側で動きを減らす設定の場合は自動的に演出を停止する
- モバイルでは黒背景のメニューパネルを開閉する。ここでも罫線は使わず、白黒反転と余白だけで階層を表現する
- 色は `#000000` と `#ffffff` のみを定義し、中間的な階層は別のグレー色ではなく透明度で表現する
- フォーカス表示はキーボード操作に必要なアクセシビリティ要素として残すが、セクション区切りの罫線・下線・点線は引き続き使用しない

## 2026-08 レスポンシブ設計の確定事項

- 画面幅は固定デバイス名ではなく、コンテンツが崩れ始める位置を基準に調整する
- 980px以下ではデスクトップナビゲーションの間隔を圧縮し、820px以下では黒背景の開閉メニューへ切り替える
- 開閉メニューはタブレットで3列、560px以下で2列とし、各リンクのタップ領域を44px以上確保する
- メニュー非表示時はリンクにキーボードフォーカスが入らないよう制御し、Escapeキーで閉じられるようにする
- Catalog / Artistsは全画面幅で同一のグリッド定義を共有する。560px以下では2列を維持し、タイトルは安全に折り返す
- Homeの最新リリースはタブレットでは2列、560px以下では1列へ切り替える
- Movieは利用可能幅に応じた自動グリッドとし、560px以下では1列表示にする
- ノッチやホームインジケータのある端末向けに `safe-area-inset-*` を考慮する
- 低い横向き画面ではHeroの高さと上下余白を圧縮し、最初の画面内に主要内容が収まりやすくする
- 画像には固有寸法を設定してレイアウトシフトを防ぎ、一覧画像は遅延読み込みする
- 320px幅を最小確認基準とし、文字やメールアドレスなどの長い内容は画面外にはみ出さないよう折り返す

## 2026-08 Next.js移行後の技術方針

- 静的HTML版の見た目と操作を維持したまま、Next.js App Router、React、TypeScript、Tailwind CSSへ移行した
- 一般公開ページはビルド時に静的生成し、通常の閲覧にSSRを必要としない構成にする
- ヘッダーとフッターはReactコンポーネントとしてHTMLに含め、旧版のようにJavaScript実行後に挿入しない
- JavaScriptを必要とする範囲は、モバイルメニュー、MOTION ON / OFF、フェード、ページ遷移、Heroの微細な反応に限定する
- Tailwind CSSは導入済みだが、SLP独自の視覚規則を優先する。色付きプリセット、角丸カード、影、罫線を安易に追加しない
- shadcn/ui互換の構成は操作部品にだけ使用する。CatalogやArtistsのカードをshadcn/ui標準デザインへ置き換えない
- ArtistsとCatalogの一覧データは `data/artists.ts` と `data/releases.ts` で管理する
- 詳細ページを追加するときは、旧URLを維持し、Next.jsの `generateMetadata` と静的生成を使用する
- 旧静的HTML版は `reference-static/` に比較用として残す。ここは編集・公開対象ではない
