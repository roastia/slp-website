# Claudeへ渡す完成プロンプト

以下の本文をそのままClaudeへ貼り付け、このZIPを添付してください。

---

添付したZIPは、福岡のレコードレーベル「SLP」のNext.jsウェブサイトです。

これは新規デザイン制作ではなく、承認済みデザインを維持しながら、未完成のArtist詳細ページとCatalog詳細ページを完成させる引き継ぎ作業です。

## 最初に行うこと

実装を始める前に、ZIP内の以下を必ず全文読み、内容を理解してください。

1. `CLAUDE.md`
2. `DESIGN_CONCEPT.md`
3. `HANDOFF_START_HERE.md`
4. `CLAUDE_HANDOFF.md`
5. `README.md`

`reference-static/`は移行前の表示・実装を確認するための参照資料です。この中の古い記述を、プロジェクト直下の指示より優先しないでください。

読み終わったら、現在完成している部分、残作業、絶対に守るデザインルールを短く整理してから実装を開始してください。重大な矛盾がない限り、確認待ちで停止せず作業を進めてください。

## 今回の実装範囲

次の詳細ページをすべて作成してください。

- Artists：3組
- Catalog：8作品

対象名、旧URL、特殊なルート構成は `CLAUDE_HANDOFF.md` に記載されています。すべての旧URLを維持してください。

実装の基準：

- `app/templates/artist/page.tsx`
- `app/templates/catalog/page.tsx`
- `data/artists.ts`
- `data/releases.ts`

データファイルを詳細情報へ拡張し、内容をコンポーネントへ重複記述しないでください。

## コンテンツ取得

事実情報は、既存公式サイトを一次情報として確認してください。

https://www.slprecordings.com/

プロフィール、作品紹介、曲名、リリース日、クレジット、配信・購入リンクなどを推測または創作しないでください。確認できない情報は `[要確認]` とし、最後に一覧で報告してください。

WordPress上の使用画像は `public/images/` へローカル保存し、可能な範囲でWebPまたはAVIFへ最適化してください。画像には適切なalt、width、height、sizes、優先読み込みまたは遅延読み込みを設定してください。画像は必ずカラーのまま使用してください。

## デザイン上の絶対条件

- 現在のデザインを別案へ作り直さない
- 色は白と黒のみ。アクセントカラーを追加しない
- グレー色を追加せず、必要な階層はopacityで表現する
- セクション区切りに罫線、下線、点線、装飾的ボーダーを使用しない
- General Sans以外のフォントを追加しない
- CatalogとArtistsの共通グリッドとカード寸法を維持する
- 画像をグレースケール加工しない
- カタログ番号、連番、作品数、アーティスト数などの装飾的な数字を追加しない
- 区切りは余白、白黒反転、文字の強弱だけで表現する
- shadcn/uiの標準的な角丸、罫線、影、アクセントカラーを持ち込まない
- `MOTION ON / OFF`、ページ遷移、スクロール表示、Heroの微細な反応を維持する
- `prefers-reduced-motion`を維持する
- 320px幅から大型デスクトップまでレスポンシブ対応する
- モバイルメニュー、キーボード操作、Escapeキー操作を壊さない

## 技術・SEO条件

- Next.js App Router、React、TypeScript、Tailwind CSSの現構成を維持する
- `package-lock.json`を維持する
- Server Componentsを基本とし、操作が必要な部分だけClient Componentsにする
- 詳細ページは静的生成する
- ページごとに固有のtitle、description、H1、canonical、OGP、Xカードを設定する
- 完成した詳細ページをsitemapへ追加する
- 一覧カードを完成した内部詳細ページへ変更する
- テンプレートはnoindexのまま残す
- 完成した実ページにはnoindexを設定しない
- 不要な依存パッケージを追加しない
- デプロイ、DNS変更、本番サイト変更は行わない

## 完了前の検証

Node.js 20.9.0以上で次を実行してください。

```bash
npm ci
npm run validate
```

加えて、次を確認してください。

1. 全3アーティストと全8作品の詳細ページ
2. 旧URLと内部リンク
3. 画像参照とalt・寸法
4. ページ固有Metadataとsitemap
5. 320px、560px、820px、デスクトップのレイアウト
6. モバイルメニューとEscapeキー
7. MOTION ON / OFFとprefers-reduced-motion
8. 白黒以外の色、装飾罫線、グレースケール加工、装飾的数字が追加されていないこと

## 提出物

- 完成したNext.jsプロジェクト一式のZIP
- 変更内容一覧
- `[要確認]`一覧
- 維持した旧URL一覧
- `npm run validate`の結果

一般的なデザイン提案や既定のUIパターンと、ZIP内の指示が衝突する場合は、`DESIGN_CONCEPT.md`とプロジェクト直下の引き継ぎ文書を優先してください。

---
